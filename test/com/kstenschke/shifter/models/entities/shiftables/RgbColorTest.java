package com.kstenschke.shifter.models.entities.shiftables;

import org.junit.Ignore;
import org.junit.Test;

public class RgbColorTest {

    @Test
    public void isRgbColorString() {
        /*
        assertFalse(RgbColor.getInstance(null, null));
        assertFalse(RgbColor.getInstance(null, ""));
        assertFalse(RgbColor.getInstance("", null));
        assertFalse(RgbColor.getInstance("", ""));
        assertFalse(RgbColor.getInstance("", "#"));
        assertFalse(RgbColor.getInstance("1", "#"));
        assertFalse(RgbColor.getInstance("11", "#"));

        assertTrue(RgbColor.getInstance("111", "#"));

        assertFalse(RgbColor.getInstance("1111", "#"));
        assertFalse(RgbColor.getInstance("11111", "#"));

        assertTrue(RgbColor.getInstance("111111", "#"));
        assertTrue(RgbColor.getInstance("11aaff", "#"));
        assertTrue(RgbColor.getInstance("ffffff", "#"));

        assertFalse(RgbColor.getInstance("gfffff", "#"));
        assertFalse(RgbColor.getInstance("ffgfff", "#"));
        assertFalse(RgbColor.getInstance("ffffgf", "#"));

        assertFalse(RgbColor.getInstance("fffffff", "#"));*/
    }

    @Test
    @Ignore
    public void getShifted() {
    }
}